Django>=5.0,<6.0
gunicorn>=22.0
psycopg[binary]>=3.2
whitenoise>=6.7

qrcode>=7.4

Pillow>=10.4,<12.0
cryptography>=42.0,<47.0
# Keep Web Push inside the application's current cryptography<47 compatibility
# envelope until that dependency boundary is reviewed separately.
pywebpush>=2.2.1,<2.3
